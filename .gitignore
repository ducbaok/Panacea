# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
.next
build
out

# Turbo
.turbo

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea
.vscode
*.suo
*.user

# Android
apps/android/.gradle
apps/android/app/.gradle
apps/android/local.properties
apps/android/app/build
apps/android/build
*.apk
*.aab
*.keystore

# Prisma
packages/database/node_modules

# GraphQL codegen
packages/graphql-types/src/generated

# Runtime uploads (kept on disk, not in git)
apps/api/uploads/

# Verify harness outputs (regenerated every run)
scripts/verify/results.json
api.log

# Graphify knowledge-graph output (regenerated by /graphify)
graphify-out/

# Claude Code — local machine permissions (personal, not team)
.claude/settings.local.json

# Prompt/brief của agent — CỐ Ý ngoài git (chốt 17/08/2026, thay dòng `*.md` cũ 11/08).
# Mọi .md khác (hiến chương, spec, plan, docs) nay được track; riêng PROMPT_*.md
# người dùng muốn giữ ngoài repo. ⚠️ Nhóm này không có lịch sử git, không có đường lùi
# — 6 brief PROMPT_DOT* đã mất vĩnh viễn vì thế. Muốn giữ lâu dài: backup thủ công.
PROMPT_*.md

# ─── Terraform (infra/) ───────────────────────────────────────────────────────
# terraform.tfvars chứa mật khẩu DB + 4 secret ứng dụng.
# *.tfstate chứa giá trị sensitive ở dạng ĐỌC ĐƯỢC — tuyệt đối không commit.
infra/**/.terraform/*
infra/**/*.tfstate
infra/**/*.tfstate.*
infra/**/terraform.tfvars
infra/**/*.tfplan
# .terraform.lock.hcl thi NGUOC LAI — PHAI commit: no ghim hash provider de
# lan init sau tren may khac ra dung phien ban. Dung them vao danh sach nay.
