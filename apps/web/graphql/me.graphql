query Me {
  me {
    id
    username
    # F1 — phụ đề mức FOLLOWERS trong bộ chọn khán giả ghi số người theo dõi
    # THẬT ("1.204 người đang theo dõi bạn" của bản vẽ là số minh hoạ).
    followerCount
    email
    name
    avatarUrl
    coverUrl
    bio
    website
    isOnboarded
    locale
  }
}

# C2 — cập nhật hồ sơ. Backend: UpdateProfileInput (name/username/bio/website/
# avatarUrl/coverUrl/locale); username regex ^[a-z0-9_]{3,20}$, đổi 1 lần / 30 ngày.
# `coverUrl` thêm ở REVIEW-1 (#6) — ảnh bìa hồ sơ.
mutation UpdateProfile($input: UpdateProfileInput!) {
  updateProfile(input: $input) {
    id
    username
    email
    name
    avatarUrl
    coverUrl
    bio
    website
    isOnboarded
    locale
  }
}

# C2 — xoá tài khoản (xác nhận hai bước ở UI). Trả Boolean.
mutation DeleteAccount {
  deleteAccount
}
