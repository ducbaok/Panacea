query HomeFeed($first: Int! = 20, $after: String, $source: FeedSource) {
  homeFeed(first: $first, after: $after, source: $source) {
    source
    items {
      id
      title
      imageUrl
      thumbnailUrl
      mediumUrl
      imageWidth
      imageHeight
      savedCount
      reactionCount
      commentCount
      createdAt
      creator {
        id
        name
        username
        avatarUrl
      }
      visibility
      audienceCircleId
      expiresAt
      isSavedByViewer
      viewerReaction
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
